# --- 1. 数据库密码 ---
# 设置 PostgreSQL 密码
POSTGRES_PASSWORD=xxx

# 设置 ClickHouse 密码
CLICKHOUSE_PASSWORD=xxx

# 设置 Minio 密码
MINIO_ROOT_PASSWORD=xxx

# 设置 Redis 密码
REDIS_AUTH=xxx

# --- 2. Langfuse 核心安全密钥 ---
# 必须是一个 64 位的十六进制字符串 (32 字节)。
# 在 Linux/macOS 上运行: openssl rand -hex 32
ENCRYPTION_KEY=90e2abbf1940d5c028627e86398883703437be3a4fd1xxxxxx

# 用于会话签名的随机字符串
NEXTAUTH_SECRET=4f65e01508625195fbdacb56bbcb6337fc3b0ac238d52xxxxx

# 用于内部哈希的随机字符串
SALT=ea92f2469f59310851d1b9fb30c33915c5c30ab3d0xxxxx

# --- 3. Langfuse 数据库连接 ---
# 确保这里的密码与上面 #1 中设置的密码一致

# 确保 POSTGRES_PASSWORD 与上面一致
DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD:-postgres}@postgres:5432/postgres

# 确保 CLICKHOUSE_PASSWORD 与上面一致
CLICKHOUSE_PASSWORD=${CLICKHOUSE_PASSWORD:-clickhouse}

# 确保 MINIO_ROOT_PASSWORD 与上面一致
LANGFUSE_S3_EVENT_UPLOAD_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD:-miniosecret}
LANGFUSE_S3_MEDIA_UPLOAD_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD:-miniosecret}
LANGFUSE_S3_BATCH_EXPORT_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD:-miniosecret}

# 确保 REDIS_AUTH 与上面一致
REDIS_AUTH=${REDIS_AUTH:-myredissecret}

# --- 4. 其他设置 (保持默认即可) ---
# 定义外部访问 Langfuse UI 的 URL，用于回调等
NEXTAUTH_URL=http://localhost:3000